# Manthan brand assets (v5 verified)

Copied from `manthan_logo_pack_v5_verified`.

| Folder | Use |
|--------|-----|
| `monogram/` | App mark, favicons, VS Code icon |
| `wordmark/` | Home / headers |
| `lockups/` | Horizontal lockup variants |
| `loaders/` | Animated SVG loaders (v2 square sequential) |
| `favicons/` | Copied pack files: `favicon-base.svg` + `favicon-base-{16,24,32,48,64,128,256}px.png` |

Regenerate embedded TS strings after editing SVGs:

```bash
bun run scripts/embed-brand-assets.ts
```
